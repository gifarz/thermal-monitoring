generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL_MATINDOK")
}

model realtime {
  id     Int     @id @default(autoincrement())
  tname  String? @db.VarChar(100)
  tvalue String? @db.VarChar(100)
}
